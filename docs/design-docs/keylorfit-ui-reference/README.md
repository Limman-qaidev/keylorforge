# KeylorFit UI visual references

This directory stores Product Owner-approved visual references used while implementing the mobile UI.

## Canonical reference for UX-001

Canonical file:

`keylorfit-ui-foundation-v1.jpg`

This is the approved concept board covering the KeylorFit visual language for Home, active workout, Progress, Training Hub, Social/Ranking and representative future product surfaces.

The image is a **directional reference**, not a source of product truth. Implementation agents must read `../KEYLORFIT_VISUAL_FOUNDATION.md` before using it.

Do not infer unfinished product behavior, API contracts, ranking algorithms or persistence rules from generated mockup copy/data.

When a new visual direction is approved, add it as a versioned reference rather than silently overwriting historical references. Update the visual-foundation contract to identify which version is canonical.
