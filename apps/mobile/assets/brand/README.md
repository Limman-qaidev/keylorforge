# Mobile brand exports

These runtime assets are raster exports derived from the canonical KeylorForge G4 SVG brand masters under `/brand`.

- `keylorforge-g4-lockup-welcome.png` — white G4 mark + white KEYLOR + teal FORGE for Welcome/photo surfaces.
- `keylorforge-g4-lockup-auth.png` — yellow G4 mark + white KEYLOR + teal FORGE for dark auth surfaces.
- `keylorforge-g4-mark-white.png` — standalone white G4 mark for compact dark or blue mobile surfaces.

Do not redraw or recolor these files independently. Update the canonical SVG assets first, then regenerate runtime exports.
