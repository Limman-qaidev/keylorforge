[build-system]
requires = ["hatchling==1.27.0"]
build-backend = "hatchling.build"

[project]
name = "keylorforge-database"
version = "0.1.0"
description = "PostgreSQL and Alembic foundation for KeylorForge"
requires-python = ">=3.11,<3.14"
dependencies = [
  "alembic==1.16.4",
  "psycopg[binary]==3.3.4",
  "pydantic-settings==2.10.1",
  "sqlalchemy==2.0.42",
]

[project.optional-dependencies]
dev = [
  "pytest==8.4.1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.hatch.build.targets.wheel]
packages = ["src/keylorforge_database"]
